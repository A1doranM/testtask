import {makeStyles} from "@material-ui/core/styles/index";

export const styleForActualOrdersTab = makeStyles(theme => ({
    tabWrapper: {
        width: '100%',
    },

    tabWrapper_title: {
        fontFamily: 'Roboto',
        fontStyle: 'normal',
        fontWeight: 'bold',
        fontSize: '24px',
        lineHeight: '28px',
        padding: '30px',
    }
}));